kind: Role
apiVersion: rbac.authorization.k8s.io/v1
metadata:
  name: promtail
  namespace: logging
  labels:
    app: promtail
rules:
  - verbs:
      - use
    apiGroups:
      - extensions
    resources:
      - podsecuritypolicies
    resourceNames:
      - promtail
